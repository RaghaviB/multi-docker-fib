name: Deploy MultiDocker
on:
  push:
    branches:
      - master # check your repo, your default branch might be master!

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t raghavi/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true raghavi/react-test npm test

      - run: docker build -t raghavi/multi-client ./client
      - run: docker build -t raghavi/multi-nginx ./nginx
      - run: docker build -t raghavi/multi-server ./server
      - run: docker build -t raghavi/multi-worker ./worker

      - run: docker push raghavi/multi-client
      - run: docker push raghavi/multi-nginx
      - run: docker push raghavi/multi-server
      - run: docker push raghavi/multi-worker